class Help {
	run() {
		console.log(`logchimp [command]

Commands:
  logchimp install      Install a brand new instance of LogChimp
  logchimp version      Prints out LogChimp-CLI version
`)
	}
};

module.exports = Help;
